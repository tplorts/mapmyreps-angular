import { DistrictOrdinalPipe } from './district.pipe';

describe('DistrictOrdinalPipe', () => {
  it('create an instance', () => {
    const pipe = new DistrictOrdinalPipe();
    expect(pipe).toBeTruthy();
  });
});
